const area = (r) => {
  console.log("Circle area: ", `${r * r} π`);
};

const circleCircumference = (r) => {
  console.log("Circle circleCircumference: ", `${2 * r} π`);
};

export { area, circleCircumference };